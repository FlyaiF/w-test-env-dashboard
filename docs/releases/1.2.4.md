## 1.2.4

- 关联资源弹窗全面改进，同地址资源不再傻傻分不清：
  - 服务器、数据库均在同一行显示登录账号（如 `root@10.20.162.81`、
    `trade1@10.20.154.151:1521/orcl`）。
  - 每一行标注已引用它的环境（用于：山东主干、天津主干…）。
  - 新增搜索框，可按主机、库名、账号或环境名同时过滤两个列表；
    已勾选项在过滤时始终可见，不会被误丢。
  - 运行服务器由全屏下拉改为独立滚动列表，与数据库列表并列展示。
